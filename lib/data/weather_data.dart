class WeatherData {
  String weather;
  int humidity;
  double wind;
  int temp;
  String icon;

  WeatherData(this.humidity, this.weather, this.wind, this.temp, this.icon);
}

List<WeatherData> weatherData = [
  // WeatherData(humidity, weather, wind, temp, icon)
];
